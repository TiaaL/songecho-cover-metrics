# 客观工具方案（定稿 v3）
 
取代之前所有版本。基于逐项 review 确认后的最终方案。
 
---
 
## 变化摘要（相比 v2）
 
1. 样本量更新：5 首歌 × 6 个模型（SongEcho / ACE-Step+SA ControlNet+LoRA / ACE-Step+MuseControlLite / ACE-Step / MiniMax / Mureka）= 30 个样本
2. KCR 窗口从固定 10 秒改为人工标注的段落边界（verse/chorus/bridge）
3. chromagram 可视化改用 DAW 频谱截图（stereo 原始信号），不再用 librosa 画
4. 明确音频格式要求：不改采样率，人声 mono，伴奏 stereo，全混 stereo
5. autochord 在 Windows 上运行，Mac 上用 mock 数据调试
---
 
## 一、Pipeline 总览
 
```
Cover audio（stereo，原始采样率，不做任何预处理）
  │
  ├─→ Demucs (htdemucs_ft, --two-stems=vocals)
  │     │
  │     ├─→ vocals.wav (mono)
  │     │     └─→ basic-pitch ──→ vocals.mid
  │     │                           │
  │     │               ┌───────────┴───────────┐
  │     │               │ pretty_midi            │ music21
  │     │               │ → LLR, PR, PS (D1)     │ → IKNR (D2)
  │     │               └───────────┬───────────┘   → KC (D3)
  │     │                           │                → KCR (D3)
  │     │                           │
  │     │                 KCR 使用你标注的段落边界做窗口
  │     │
  │     └─→ no_vocals.wav (stereo)
  │           └─→ autochord ──→ 和弦序列 ──→ IKCR (D2)
  │                                            ↑
  │                              music21 提供调性基准
  │
  ├─→ pyloudnorm（stereo，原始采样率）
  │     └─→ LUFS, LRA (D5)
  │
  └─→ librosa.spectral_contrast（内部转 mono）
        └─→ SC (D5)
 
可视化：DAW 频谱截图（stereo 原始信号），不参与指标计算
```
 
---
 
## 二、数据源与格式要求
 
| 文件 | 格式 | 给谁用 | 算什么 | 注意事项 |
|------|------|--------|--------|---------|
| 原始 cover audio | stereo，原始采样率 | pyloudnorm | LUFS、LRA | 不做任何预处理 |
| 原始 cover audio | 内部转 mono | librosa | SC | 只在函数内部转，不改原文件 |
| vocals.wav | mono，Demucs 输出 | basic-pitch | → vocals.mid | 不改采样率 |
| vocals.mid | MIDI | pretty_midi | LLR、PR、PS | — |
| vocals.mid | MIDI | music21 | IKNR、KC、KCR | KCR 用你标注的段落边界 |
| no_vocals.wav | stereo，Demucs 输出 | autochord | IKCR 的和弦数据 | Windows 上运行 |
 
---
 
## 三、10 个指标的定义与计算
 
### D1 旋律合理性：3 个指标
 
**指标 1：大跳比例 (Large Leap Ratio, LLR)**
 
数据源：vocals.mid。pretty_midi 读取，提取所有音符的 pitch（MIDI note number）。计算每对相邻音符的音程 = |pitch[i+1] - pitch[i]|。音程 > 7 semitones（纯五度）算大跳。LLR = 大跳次数 / 总音程数。输出 0-1 的小数。预期与 D1 负相关。
 
```python
import pretty_midi
pm = pretty_midi.PrettyMIDI("song_vocals.mid")
notes = pm.instruments[0].notes
pitches = [n.pitch for n in notes]
intervals = [abs(pitches[i+1] - pitches[i]) for i in range(len(pitches)-1)]
LLR = sum(1 for i in intervals if i > 7) / len(intervals)
```
 
**指标 2：音域范围 (Pitch Range, PR)**
 
同一组 pitch 数据。PR = max(pitch) - min(pitch)，单位 semitone。人声合理范围约 12-24 semitones。PR 本身无明确方向性（音域宽不等于差），在 Spearman 分析中作辅助参考。
 
```python
PR = max(pitches) - min(pitches)
```
 
**指标 3：音程变化标准差 (Pitch Stability, PS)**
 
对所有音程值求标准差。PS 大 = 音程变化不规律（时而小跳时而大跳），旋律走向混乱。预期与 D1 负相关。
 
```python
import numpy as np
PS = np.std(intervals)
```
 
已知局限：basic-pitch 转录可能漏掉短时值音符（经过音、装饰音），导致相邻音符音程被人为放大，LLR 和 PS 会偏高。但此误差对所有样本是系统性的，不影响样本间排序，Spearman 相关不受影响。
 
### D2 和声合理性：2 个指标
 
**指标 4：调内音比例 (In-Key Note Ratio, IKNR)**
 
数据源：vocals.mid。music21 加载 MIDI → `analyze('key')` 检测全曲调性（Krumhansl-Schmuckler 算法）→ 遍历每个音符，判断 pitch class（pitch % 12）是否属于该调音阶的 7 个音。IKNR = 调内音数 / 总音符数。输出 0-1 的小数。预期与 D2 正相关。IKNR 同时也与 D3 相关（调内音比例是调性一致性的侧面）。
 
```python
import music21
score = music21.converter.parse("song_vocals.mid")
key = score.analyze('key')
key_pitches = [p.midi % 12 for p in key.getScale().getPitches()]
in_key = sum(1 for p in pitches if (p % 12) in key_pitches)
IKNR = in_key / len(pitches)
```
 
**指标 5：调内和弦比例 (In-Key Chord Ratio, IKCR)**
 
数据源：no_vocals.wav（伴奏轨，stereo）。autochord 直接输入音频（不需要转 MIDI），输出和弦序列 [(start, end, 'X:maj/min'), ...]。然后用 music21 检测到的全曲调性，判断每个和弦根音是否属于该调的自然音级。IKCR = 调内和弦数 / 总和弦数。预期与 D2 正相关。
 
```python
import autochord
chords = autochord.recognize("no_vocals.wav")
 
key_obj = music21.key.Key(key.tonic.name, key.mode)
diatonic_roots = [p.name for p in key_obj.getScale().getPitches()[:-1]]
 
in_key_count = 0
total_count = 0
for start, end, chord_label in chords:
    if chord_label == 'N':
        continue
    root = chord_label.split(':')[0]
    total_count += 1
    if root in diatonic_roots:
        in_key_count += 1
IKCR = in_key_count / max(total_count, 1)
```
 
已知局限：autochord 精度约 67%，只识别大小三和弦。只判断根音是否在调内，不判断和弦功能进行是否合理。和弦功能逻辑由 D2 人工评分覆盖。autochord 不支持 Mac (Apple Silicon)，需在 Windows 上运行。Mac 开发时用 mock 数据调试后续代码。
 
### D3 调性一致性：2 个指标
 
**指标 6：调性置信度 (Key Confidence, KC)**
 
数据源：vocals.mid。music21 `analyze('key')` 返回的 Key 对象有一个 `correlationCoefficient` 属性，范围 0-1，表示音级分布与最佳匹配调性模板的 Pearson 相关系数。KC 高 = 调性中心明确。预期与 D3 正相关。
 
```python
KC = key.correlationCoefficient
```
 
**指标 7：调性变化率 (Key Change Rate, KCR)**
 
数据源：vocals.mid + 你标注的段落边界。按你标注的段落（verse1/chorus1/bridge/...）切分音符，对每个段落独立做 music21 调性检测。得到段落调性序列，统计相邻段落调性变化次数。KCR = 变化次数 / (段落数 - 1)。预期与 D3 负相关。
 
```python
# segments 来自你的标注表，格式 [(start_sec, end_sec, label), ...]
keys_per_segment = []
for seg_start, seg_end, seg_label in segments:
    seg_notes = [n for n in notes if n.start >= seg_start and n.start < seg_end]
    if len(seg_notes) < 5:  # 音符太少则跳过，检测不可靠
        continue
    seg_stream = music21.stream.Stream()
    for n in seg_notes:
        seg_stream.append(music21.note.Note(n.pitch))
    seg_key = seg_stream.analyze('key')
    keys_per_segment.append(str(seg_key))
 
changes = sum(1 for i in range(len(keys_per_segment)-1)
              if keys_per_segment[i] != keys_per_segment[i+1])
KCR = changes / max(len(keys_per_segment)-1, 1)
```
 
已知局限：合理转调（副歌升半调）也会被计入变化。但 AI 生成音乐中频繁调性变化通常是无意义跳调。段落内音符少于 5 个的段落跳过计算。
 
### D5 编曲与制作质量：3 个指标
 
**指标 8：综合响度 (Integrated Loudness, LUFS)**
 
数据源：原始 cover audio（stereo，原始采样率，不做任何预处理）。pyloudnorm 按 ITU-R BS.1770-4 标准计算。输出 dB LUFS。Spearman 分析时用偏差值 LUFS_dev = |LUFS - (-12)|。预期 LUFS_dev 与 D5 负相关。
 
```python
import soundfile as sf
import pyloudnorm as pyln
 
data, rate = sf.read("song.wav")  # stereo, 原始采样率
meter = pyln.Meter(rate)
LUFS = meter.integrated_loudness(data)
LUFS_dev = abs(LUFS - (-12))
```
 
已知局限：同一模型的所有输出响度可能很接近，LUFS_dev 区分度可能很小。
 
**指标 9：响度范围 (Loudness Range, LRA)**
 
数据源同上。pyloudnorm 计算，单位 LU。流行音乐正常范围 5-15 LU。LRA 过低 = 过度压缩，LRA 过高 = 声部失控。预期与 D5 正相关。
 
```python
LRA = meter.loudness_range(data)
```
 
**指标 10：频谱对比度 (Spectral Contrast, SC)**
 
数据源：原始 cover audio。librosa 计算时内部转 mono（不影响其他指标的 stereo 计算）。spectral_contrast 把频谱分成 7 个子频段，计算峰谷差，取所有帧和频段的均值。SC 高 = 声部频谱分离清晰，SC 低 = 频谱浑浊。预期与 D5 正相关。
 
```python
import librosa
y, sr = librosa.load("song.wav", sr=None, mono=False)
if y.ndim > 1:
    y_mono = librosa.to_mono(y)
else:
    y_mono = y
contrast = librosa.feature.spectral_contrast(y=y_mono, sr=sr)
SC = np.mean(contrast)
```
 
已知局限：SC 是声部分离度的粗略近似。D5 评分同时包含编曲维度（纯人工）和制作维度，客观指标只覆盖制作维度，Spearman 相关可能偏弱。
 
### D4 风格一致性：无客观指标
 
纯人工评分。原因：风格匹配需要语义理解（"这听起来像不像 rock"），现有 audio 信号处理工具做不到。
 
---
 
## 四、D5 评分标准（更新版）
 
D5 名称改为：**编曲与制作质量 / Arrangement & Production Quality**
 
| 分数 | 编曲维度 | 制作维度 |
|------|---------|---------|
| 5 | 织体极其清晰，声部平衡，层次丰富且动态变化合理 | 频谱平衡，响度适当，动态自然，无伪影 |
| 4 | 织体清晰，声部平衡，层次分明 | 频谱基本平衡，偶有轻微问题但不影响听感 |
| 3 | 整体合理，偶有声部略模糊或层次不够分明 | 响度或频谱有可察觉偏差，但整体可接受 |
| 2 | 声部明显打架，织体混乱，层次缺失 | 明显过压缩/频谱失衡/clipping/AI伪影 |
| 1 | 编曲严重混乱，声部堆叠/缺失，无法支撑歌曲 | 严重失真、爆音、金属感、持续噪声 |
 
打分时综合两个维度给一个 D5 总分，不拆分。
 
---
 
## 五、完整指标表
 
| 序号 | 指标 | 缩写 | 维度 | 数据源 | 来源工具 | 预期方向 |
|------|------|------|------|--------|---------|---------|
| 1 | 大跳比例 | LLR | D1 | vocals.mid | pretty_midi | 负相关 |
| 2 | 音域范围 | PR | D1 | vocals.mid | pretty_midi | 辅助参考 |
| 3 | 音程变化标准差 | PS | D1 | vocals.mid | pretty_midi | 负相关 |
| 4 | 调内音比例 | IKNR | D2/D3 | vocals.mid | music21 | 正相关 |
| 5 | 调内和弦比例 | IKCR | D2 | no_vocals.wav | autochord+music21 | 正相关 |
| 6 | 调性置信度 | KC | D3 | vocals.mid | music21 | 正相关 |
| 7 | 调性变化率 | KCR | D3 | vocals.mid+段落标注 | music21 | 负相关 |
| 8 | 综合响度偏差 | LUFS_dev | D5 | 原始audio(stereo) | pyloudnorm | 负相关 |
| 9 | 响度范围 | LRA | D5 | 原始audio(stereo) | pyloudnorm | 正相关 |
| 10 | 频谱对比度 | SC | D5 | 原始audio(转mono) | librosa | 正相关 |
 
---
 
## 六、Spearman 相关分析
 
```python
import pandas as pd
from scipy.stats import spearmanr
 
df = pd.read_csv("results.csv")
df['LUFS_dev'] = abs(df['LUFS'] - (-12))
 
correlations = [
    ('D1', 'LLR', '负'),
    ('D1', 'PS', '负'),
    ('D2', 'IKNR', '正'),
    ('D2', 'IKCR', '正'),
    ('D3', 'KC', '正'),
    ('D3', 'IKNR', '正'),
    ('D3', 'KCR', '负'),
    ('D5', 'LUFS_dev', '负'),
    ('D5', 'LRA', '正'),
    ('D5', 'SC', '正'),
]
 
for dim, metric, direction in correlations:
    rho, p = spearmanr(df[dim], df[metric])
    sig = '***' if p < 0.001 else '**' if p < 0.01 else '*' if p < 0.05 else 'n.s.'
    print(f"{dim} vs {metric}: ρ={rho:.3f}, p={p:.3f} {sig} (预期{direction}相关)")
```
 
结果不论显著与否都报告。n=30 时 |ρ| ≈ 0.36 即可达到 p<0.05。
 
---
 
## 七、可视化
 
论文配图用 DAW 频谱截图（Logic Pro / Cubase / SPAN），保留 stereo 原始信号。选 3-4 个典型案例（高分 vs 低分各 1-2 个）。Figure caption 示例：
 
"Figure X: Frequency spectrum of Song 1 covers. Left: ModelA (D5=2, spectral energy concentrated in low-mid range with poor separation). Right: ModelB (D5=4, balanced spectral distribution across frequency bands)."
 
---
 
## 八、工具栈
 
```bash
# Mac 上安装（开发+调试）
pip install librosa basic-pitch pretty-midi music21 scipy matplotlib pandas
pip install demucs
pip install pyloudnorm soundfile
 
# Windows 上额外安装（跑 autochord）
pip install autochord
```
 
| 工具 | 用途 | 运行环境 |
|------|------|---------|
| Demucs (htdemucs_ft) | 人声/伴奏分离 | Mac |
| basic-pitch | 人声 → MIDI | Mac |
| pretty_midi | 读 MIDI、算 D1 指标 | Mac |
| music21 | 调性检测、调内音/和弦判断 | Mac |
| autochord | 伴奏和弦识别 | **Windows** |
| pyloudnorm | LUFS + LRA | Mac |
| librosa | SC | Mac |
| scipy.stats | Spearman 相关 | Mac |
| pandas + matplotlib | 汇总 + 画图 | Mac |
| DAW (Logic/Cubase/SPAN) | 频谱截图 | Mac |
 
---
 
## 九、时间线
 
| 事项 | 耗时 | 备注 |
|------|------|------|
| 安装所有工具（Mac + Windows） | 0.5 天 | |
| 补标段落起止时间（30 个样本） | 1 天 | 用于 KCR |
| 30 个样本跑 Demucs + basic-pitch | 0.5 天 | |
| 30 个样本提取 9 个指标（除 IKCR） | 1 天 | Mac 上完成 |
| 30 个样本跑 autochord + 算 IKCR | 0.5 天 | Windows 上跑 autochord |
| Spearman 分析 + DAW 频谱截图 | 0.5 天 | |
| **总计** | **4 天** | |
 
---
 
## 十、不要做的事
 
- 不要改音频采样率
- 不要把 stereo 音频转 mono 再给 pyloudnorm（LUFS/LRA 必须 stereo）
- 不要试图提高 autochord 或 basic-pitch 的精度（不是你的研究目标）
- 不要给 D4 硬加客观指标
- 不要在工具上花超过 4 天
- 不要再改这个方案

【注意】一句修改: "KCR不用段落标注,改回固定10秒窗口"
 -------
音频路径/Users/xy/Desktop/1/code/music/english
 跑通一个验证，自测合格再交付
-------
输出:
一个单文件脚本,输入一个cover音频路径,输出10个指标数值。
别让它一次性写一个处理30个文件的复杂pipeline——先确保单个文件能跑通、数值合理,再批量。